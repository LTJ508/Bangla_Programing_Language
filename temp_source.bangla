শুরু
{
    //File operations
    ফাইল ফাইল১;
    ফাইল১ = খুলো ("নতুনফাইল.txt", লেখো);
    লেখো (ফাইল১ , "JOY KUMAR GHOSH");
    দেখাও("ফাইলে এক লাইন লেখা হয়েছে", নতুন_লাইন);

    বন্ধ (ফাইল১);
    দেখাও(নতুন_লাইন);

    ফাইল ফাইল২ = খুলো ("নতুনফাইল.txt", পড়ো);
    ধরি স্ট্রিইম;
    স্ট্রিইম = পড়ো (ফাইল২);
    দেখাও (স্ট্রিইম, নতুন_লাইন);

    বন্ধ (ফাইল২);
}
শেষ;